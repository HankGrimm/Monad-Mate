[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "monadmate-stake-sdk"
version = "0.1.0"
description = "Stake-gated access control for Web3 social apps — stake lifecycle, slashing policy, no-show multipliers"
readme = "README.md"
license = { text = "MIT" }
authors = [{ name = "HankGrimm", email = "HankGrimm91@gmail.com" }]
keywords = ["stake", "monad", "mon", "web3", "social", "access-control", "slashing", "escrow"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Topic :: Software Development :: Libraries",
]
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7"]

[project.urls]
Homepage = "https://github.com/HankGrimm/monad-mate-trust-api"
Repository = "https://github.com/HankGrimm/monad-mate-trust-api/tree/main/packages/monadmate-stake-sdk"
