[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "monadmate-reputation"
version = "0.1.0"
description = "Portable on-chain reputation decay with Hedera HCS anchoring for Web3 social apps"
readme = "README.md"
license = { text = "MIT" }
authors = [{ name = "HankGrimm", email = "HankGrimm91@gmail.com" }]
keywords = ["reputation", "hedera", "hcs", "monad", "web3", "social", "decay", "trust"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Topic :: Software Development :: Libraries",
]
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.24.0",
]

[project.optional-dependencies]
dev = ["pytest>=7", "pytest-asyncio>=0.21"]

[project.urls]
Homepage = "https://github.com/HankGrimm/monad-mate-trust-api"
Repository = "https://github.com/HankGrimm/monad-mate-trust-api/tree/main/packages/monadmate-reputation"
