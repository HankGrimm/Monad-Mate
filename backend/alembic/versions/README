Alembic migration versions for Monad Mate Trust API.

Generate a new migration:
    alembic revision --autogenerate -m "describe change"

Apply migrations:
    alembic upgrade head

Rollback one step:
    alembic downgrade -1

View history:
    alembic history --verbose

Current revision:
    alembic current
